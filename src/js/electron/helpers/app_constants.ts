const { app } = require('electron');

export abstract class AppConstants {
    public static readonly VERSION: string = '0.0.1';
    public static readonly CRED_FILE: string = '';
    // + '/cred.json'

}
